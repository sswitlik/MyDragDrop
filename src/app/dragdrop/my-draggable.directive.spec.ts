import { MyDraggableDirective } from './my-draggable.directive';

describe('MyDraggableDirective', () => {
  it('should create an instance', () => {
    const directive = new MyDraggableDirective();
    expect(directive).toBeTruthy();
  });
});
